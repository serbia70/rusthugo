+++
title = "临时命令"
date = "2026-09-16"
draft = false

[taxonomies]
tags = []
series = []
+++
按下面顺序执行，一条一条执行即可：

sudo umount -R /mnt 2>/dev/null

sudo mkdir -p /mnt

sudo mount /dev/sda1 /mnt

sudo mkdir -p /mnt/boot/efi

sudo mount /dev/sda4 /mnt/boot/efi

然后确认：

lsblk -f

mount | grep /mnt

如果 sda1 是 Windows/Linux 系统分区、sda4 是 EFI 分区，再继续。

如果你的目标是修复 Debian 的 UEFI 启动

sudo mount --bind /dev /mnt/dev

sudo mount --bind /dev/pts /mnt/dev/pts

sudo mount --bind /proc /mnt/proc

sudo mount --bind /sys /mnt/sys

sudo mount --bind /run /mnt/run

sudo chroot /mnt

进入 chroot 后：

apt update

apt install --reinstall grub-efi-amd64 shim-signed

grub-install --target=x86_64-efi --efi-directory=/boot/efi --bootloader-id=debian --recheck

update-grub

然后：

exit

sudo umount -R /mnt

sudo reboot

