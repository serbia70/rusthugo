+++
title = "每天检查"
date = "2026-06-02"
draft = false

[taxonomies]
tags = []
series = []
+++
docker restart cloudflare-tunnel

docker exec -it cloudflare-tunnel ping -c 3 headscale

docker logs cloudflare-tunnel --tail=20 | grep hostname

docker logs cloudflare-tunnel --tail=20 | grep "ingress"

docker logs cloudflare-tunnel --tail=20 | sed -n '/Updated to new configuration/,$p'

docker logs headscale --tail=20

docker logs nginx-proxy-manager --tail=20

docker exec -it central-mqtt-broker cat /mosquitto/log/mosquitto.log | tail -n 20

docker ps | grep -E "meituan|order|food-server"

docker logs -f cloudflare-tunnel

docker logs -f headscale

重启

docker restart cloudflare-tunnel

docker restart nginx-proxy-manager

docker restart headscale 

docker logs -f headscale 

journalctl -u tailscaled -f

宿主机劫持了  （最终解决）

cat /etc/hosts

删除
sed -i '/headscale.serbia70.com/d' /etc/hosts



切断网络防止报错

docker network disconnect shop-network nginx-proxy-manager

恢复网络

docker network connect shop-network nginx-proxy-manager

查询网址headscale

docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' $(docker ps -q --filter "name=headscale")


# 1. 一键停止两个容器
docker stop headscale nginx-proxy-manager

# 2. 先启动被反代的后端（Headscale）
docker start headscale

# 3. 等待 5 秒钟，让 Headscale 彻底把网络端口拉起来
sleep 5

# 4. 再启动前端反代（NPM）
docker start nginx-proxy-manager

