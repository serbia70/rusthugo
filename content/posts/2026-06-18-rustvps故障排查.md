+++
title = "rustvps故障排查"
date = "2026-06-19"
draft = false

[taxonomies]
tags = []
series = []
+++

查询ip和端口
```
docker port central-mqtt-broker
```

打印机
```
chmod +x /opt/printer-n1/printer-arm64 && systemctl restart printer-n1 && journalctl -u printer-n1.service -f
```

vps订单查询
```
docker logs food-server-test 2>&1 | grep -i "mqtt" | tail -5
```


Caddy 热重载过旧配置
```
docker exec -it caddy cat /etc/caddy/Caddyfile
```
查询容器ip
```
docker inspect central-mqtt-broker | grep -i ipaddress
```

查询最近10个错误
```
docker logs food-server-test 2>&1 | grep -i "error\|500" | tail -10
```

 mqtt2 直连 mosquitto
```
sed -i '/hostname: mqtt2.serbia70.com/{n;s|service: http://caddy:80|service: http://central-mqtt-broker:8083|;}'
/opt/caddy/cloudflared/config.yml
grep -A1 mqtt2 /opt/caddy/cloudflared/config.yml
```

重建所有容器
```
cd /opt/caddy && docker compose up -d --force-recreate
```

sleep 8

cloudflared 确认配置生效
```
docker logs cloudflare-tunnel 2>&1 | grep "mqtt2" | tail -3
```






