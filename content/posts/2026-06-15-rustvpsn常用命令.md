+++
title = "rustvpsn常用命令"
date = "2026-06-16"
draft = false

[taxonomies]
tags = []
series = []
+++

三大件
```
docker logs --since 120m central-mqtt-broker && docker logs --since 120m caddy && docker logs --since 120m cloudflare-tunnel
```

项目
```
docker logs --since 120m food-server && docker logs --since 120m food-server-test
```
正式版三大件
```
docker logs --since 120m central-mqtt-broker && docker logs --since 120m nginx-proxy-manager && docker logs --since 120m cloudflare-tunnel
```
正式版项目
```
docker logs --since 120m food-server && docker logs --since 120m meituan7
```





最近10小时

docker logs --since 600m central-mqtt-broker

docker logs --since 600m food-server-test

docker logs --since 600m  caddy

docker logs --since 6000m cloudflare-tunnel

最近1小时

docker logs --since 60m central-mqtt-broker

docker logs --since 60m  caddy

docker logs --since 60m cloudflare-tunnel

docker logs --since 60m food-server-test


50行最近的包括实时

docker logs -f --tail=50 central-mqtt-broker

docker logs -f --tail=50 food-server-test

docker logs -f --tail=50 cloudflare-tunnel

docker logs -f --tail=50 caddy

关闭

docker stop food-server-test

重启 

docker restart food-server-test


拉取

docker compose pull && docker compose up -d food-server-test







